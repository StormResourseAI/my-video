// Vitest stub: the real `server-only` package throws on import outside a
// React Server context. Unit tests exercise studio/server modules in Node,
// where the boundary is enforced by tests/safety.test.tsx instead.
export {};
